# 🛒 افزونه‌ی اتصال فروشگاه به سایت اصلی (SPFCW)

این افزونه به مدیران فروشگاه‌ها اجازه می‌دهد تا محصولات موجود در **سایت اصلی** را تنها با چند کلیک به فروشگاه وردپرسی خود اضافه کنند.  
به‌صورت تکی یا گروهی می‌توانید محصولات را ایمپورت کنید و به‌صورت خودکار اطلاعات، قیمت و تصویر محصول تنظیم می‌شود.

---

## ⚙️ ویژگی‌ها

✅ دریافت لیست محصولات از سایت اصلی  
✅ افزودن محصول تکی با یک کلیک  
✅ افزودن همه محصولات به‌صورت خودکار  
✅ ذخیره‌ی شناسه محصول اصلی برای همگام‌سازی بعدی  
✅ پشتیبانی از محصولات خارجی (External Products)  
✅ افزودن خودکار تصویر شاخص (Thumbnail)  
✅ بخش تنظیمات برای تعریف آدرس سایت اصلی و API Key  

---

## 📁 ساختار پوشه‌ها

spfcw/
│
├── spfcw.php # فایل اصلی افزونه
├── includes/
│ ├── class-admin.php # کلاس مدیریت (صفحه ادمین و ایمپورت محصولات)
│ ├── class-api-client.php # ارتباط با سایت اصلی (API)
│ └── class-loader.php # لودر افزونه (اتصال کلاس‌ها)
└── README.md # این فایل 


---

## 🔧 نصب و راه‌اندازی

1. پوشه‌ی افزونه را در مسیر زیر قرار دهید:
wp-content/plugins/spfcw

2. وارد پیشخوان وردپرس شوید و افزونه را فعال کنید.

3. از منوی سمت راست روی گزینه **SPFCW → تنظیمات** کلیک کنید.

4. در صفحه تنظیمات:
- آدرس سایت اصلی را وارد کنید (مثلاً `https://example.com`)
- اگر لازم بود، API Key را بنویسید. (در صورت غیرفعال بودن محدودیت، می‌تواند خالی بماند)

5. تنظیمات را ذخیره کنید ✅

---

## 🚀 استفاده از افزونه

### ➊ مشاهده محصولات سایت اصلی
از منوی مدیریت وردپرس وارد **SPFCW** شوید.  
در این صفحه لیست محصولات سایت اصلی را مشاهده می‌کنید.

### ➋ افزودن محصول تکی
روی دکمه‌ی **➕ افزودن به فروشگاه من** کلیک کنید تا محصول انتخابی وارد فروشگاه شما شود.

### ➌ افزودن همه محصولات
با کلیک بر روی دکمه‌ی **🚀 افزودن همه محصولات**، افزونه به‌صورت خودکار تمام محصولات سایت اصلی را به فروشگاه شما اضافه می‌کند.

---

## 🕒 نکته درباره‌ی کرون (Cron)

افزودن گروهی محصولات از طریق سیستم زمان‌بندی وردپرس (WP-Cron) انجام می‌شود.  
برای عملکرد صحیح، باید کرون فعال باشد:

- در فایل `wp-config.php` مطمئن شوید خط زیر **وجود ندارد** یا روی `false` تنظیم شده است:

```php
define('DISABLE_WP_CRON', true);
```
در صورت استفاده از Docker، پیشنهاد می‌شود یک کرون‌جاب واقعی تنظیم کنید:
```
*/5 * * * * wget -q -O - http://yourdomain.com/wp-cron.php?doing_wp_cron >/dev/null 2>&1
```

⚠️ نکات امنیتی

اگر سایت اصلی عمومی است و محدودیتی ندارد، نیازی به API Key نیست.

در غیر این صورت، از یک کلید API امن برای احراز هویت استفاده کنید.

این افزونه تنها برای ادمین‌ها در دسترس است (سطح دسترسی manage_options).


📜 لایسنس

این پروژه تحت مجوز MIT License منتشر شده است.
آزادید از آن در پروژه‌های تجاری یا شخصی استفاده کنید، تنها نام توسعه‌دهنده را حفظ کنید.👨‍💻 توسعه‌دهنده

طراح و توسعه‌دهنده: حسنعلی عسکری

 [برای رفتن به گیت هابم روی متن کلیک کنید](https://github.com/hassan7303)